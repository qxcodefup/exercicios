### C
```c
#define True 1
#define False 0

int eh_quadrado_perfeito(int numero){
      for (int value =0; value <= numero; value++)
            if (value * value == n)
                  return True
      return False
}
```
### Python
```py
def eh_quadrado_perfeito(numero):
      for value in range(n + 1):
            if value * value == n:
                  return True
      return False
```
